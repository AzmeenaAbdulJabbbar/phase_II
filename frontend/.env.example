# API Configuration
# Backend API URL (FastAPI running on port 8000)
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# Better Auth Configuration
# CRITICAL: This secret MUST match the backend BETTER_AUTH_SECRET
# Generate a secure random string (min 32 characters)
# Example: openssl rand -base64 32
BETTER_AUTH_SECRET=your-secure-secret-min-32-characters-replace-this

# Database Configuration
# PostgreSQL connection URL for Better Auth session storage
# Format: postgres://user:password@host:port/database
DATABASE_URL=postgres://user:password@localhost:5432/todo_app

# Node Environment
NODE_ENV=development
